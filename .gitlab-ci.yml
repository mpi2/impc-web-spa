default:
  image: node:lts-bookworm

pages:
  stage: deploy
  script:
    - yarn install
    - yarn build
    - /* /index.html 200 >> $CI_PROJECT_DIR/public/_redirects
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
  environment: production