default:
  image: node:lts-bookworm

pages:
  stage: deploy
  script:
    - yarn install
    - yarn build
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
  environment: production